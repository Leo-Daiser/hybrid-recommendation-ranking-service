ranking:
  input:
    interactions_path: "data/processed/train_interactions.parquet"
    candidates_path: "artifacts/candidates/itemknn_candidates.parquet"
    user_features_path: "data/processed/user_features.parquet"
    item_features_path: "data/processed/item_features.parquet"
    
  output:
    train_ranking_path: "data/processed/train_ranking.parquet"
    valid_ranking_path: "data/processed/valid_ranking.parquet"
    
  dataset:
    # Sampling parameters
    negative_sample_ratio: 5
    
  columns:
    user_id: "user_id"
    item_id: "item_id"
    label: "label"
    score: "retrieval_score"
